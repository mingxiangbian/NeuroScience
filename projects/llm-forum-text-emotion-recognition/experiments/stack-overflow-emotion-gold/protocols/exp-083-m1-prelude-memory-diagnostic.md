# EXP-083：完整 M1 前置任务后的首次 M3 诊断

Date：2026-08-31。Tier：Minor。RQ-S3；合并到 EXP-079 的资源诊断记录。
用户要求继续。EXP-082 已完成七项短诊断，但没有前置完整 M1 任务；本次补上这段负载，
不改模型，也不把短诊断晋升为九任务验收。

## 一次有限序列

固定顺序：M1-only 340项 → 确认正常退出与全部已见进程消失 → 原安静窗口 → Research前7项。
两个推理进程必须串行，最多347个结果回执；M1预算0，Research预算1，audit0，seed42。
前置任务包括它自己的资产核验、M1计算、缓存及进程退出，不把它简称为纯模型计算干预。

输入仍为已验证 source job `5ab3326150ee448ba326233264967d34` 的340项原序文本；
snapshot SHA=`cd656b035e76c9b4916c80b864a4f923de08c67af8863c426b3f774ec1c78a16`。
Research仅ordinals0–6，前六项路由资格false、第七项true，与EXP-082完全相同。
本次可以只读该已登记快照全部340项及既有结果作身份/复算，不访问其他原文或gold。

总预算180秒，工作最多150秒，清理最多15秒，监控/封存预留15秒。每个进程启动前最多
等待60秒，并须满足10个连续normal/无已知活子进程/低swap I/O样本。保留critical即停、
swap至少100MiB/s连续3间隔即停、RSS12GiB、MLX10GB、父进程1GiB、磁盘512MiB以及
身份、并发、孤儿、退出检查。任何失败阻止后续阶段，不自动修复或重试。
只读等待可以如实登记NotReady；这不算启动Research，也不能据此声称第二个推理进程已经执行。

## 固定执行与新增观察

继续使用EXP-082的直接子进程入口、原ProcessRunner与Monitor，不在本次同时改变HTTP入口。
M1前置任务调用原inference_process.main，Research复用已验证的EXP-082阶段观察器，
仅以新薄适配器把journal路径定向到EXP-083。旧代码、旧协议和旧工件均不修改。
模型、权重、精度、tokenizer、prompt、截断、阈值、router及成功路径的数值执行不变。
不调cache、不新增eval、不重置峰值或设置wired limit。

Research记录与EXP-082相同的阶段/内存字段；M1前置阶段只用原回执和过程观测，不扩展追踪算法。
全部journal私有、create-only，禁止原文/tensor值/异常文本进入阶段或公开日志。
未采样不是0；限额设置后必要内存计数不可取得或超过原阈值，同样保存事实后停止。

## 证据与边界

只写`forum-topic-emotion-web/private/validation/exp-083/attempt-1/`，绑定EXP-082的
plan/run/verification真实hash、原全部依赖、新增代码及测试、本协议和340项输入身份。
先完成合成与安全检查，再执行一次；父进程退出后进行无模型独立核验。
完整序列要求340+7回执、两次正常退出、原安全门与Research阶段记录全部通过。
M1预期338次计算+2缓存；Research预期7次M1+1次M3。未完成部分只报告已确认成本下界。

比较EXP-082时，描述是否复现停止、发生时的已保存阶段和计数器变化。两次运行时间和整机状态
未受控，且读取/核验前置输入也属于新增负载，因此单次结果不能证明前置M1任务导致或不导致
内存压力。它仍不是原HTTP流程、三轮三模式或生产稳定性的完整复现。
保持原EXP-079未完成、EXP-080未执行；本次不自动启动它们。

没有训练、validation/test访问、外部gold、context/C2、新数据采集或上传、stage/commit/push。
